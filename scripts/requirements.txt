# JN Grader Python dependencies
#
# Install: pip install -r requirements.txt
# Python >= 3.10 required

# --- LLM clients ---
openai>=1.0.0            # OpenAI-compatible client (default provider; also embeddings.py)
anthropic>=0.39.0        # Native Claude SDK (--provider anthropic)

# --- Config ---
PyYAML>=6.0              # config.yaml / rubric.yaml parsing

# --- Task bank (Postgres + pgvector) ---
psycopg[binary]>=3.1   # Postgres client for db_ingest.py / db_query.py

# --- Notebook support ---
nbformat>=5.7.0          # Jupyter notebook parsing
nbclient>=0.9.0          # Whole-notebook execution (preprocess.py)
jupyter>=1.0.0           # Jupyter kernel (provides the python3 kernel)
numpy>=1.24.0            # Array comparison (run_tests / physics_checks / field_checks)
matplotlib>=3.7.0        # Agg backend for plotting + field_checks curve capture

# --- Tests ---
pytest>=7.0.0            # Test runner
